#include <iostream>
#include <vector>
typedef long int lli;
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  vector<int> candies(n);
  for (int i = 0; i < n; i++) {
    cin >> candies[i];
  }
  int i = 0, last;

  return 0;
}
